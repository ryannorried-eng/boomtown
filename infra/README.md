# Infrastructure Notes

Primary local orchestration is defined in the repository root `docker-compose.yml`.

This directory is reserved for future IaC assets (cloud manifests, scheduler configs, observability, etc.).
